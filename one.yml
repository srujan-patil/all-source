---

- hosts : dev 
  connection: ssh

  tasks:

    - name: install hhtpd
      yum : name=httpd state=present

    - name: content
      file:
        path: "/var/www/html/index.html"
        state: touch

    - name: content deploy
      copy:
        dest: "/var/www/html/index.html"
        content: |
          <h1>the test file is deployed bro as itis </h1> 

    - name: start hhtpd
      service: name=httpd state=started

...
